import styled from "styled-components"

export const NavTabs = styled.div`
    padding: 0;
    box-shadow: ${({ theme }) => theme.boxShadow};
    border-radius: 5px;
    margin: 10px 0;
    width: 100%;
`;